# Жертва дня — Telegram-бот для рабочих чатов

## Описание

🤖 **Жертва дня** — это Telegram-бот для групповых чатов, который ежедневно случайным образом выбирает “жертву дня” из участников чата. Бот позволяет разнообразить рабочую рутину, назначать “ответственных” за задачи, поддерживать командный дух и вести шуточную статистику!

- Гибкая система прав: владелец, доверенные пикеры, участники
- Настраиваемое количество жеребьёвок в сутки
- Исключения, кастомные фразы, статистика, напоминания
- Управление всем функционалом через команды Telegram

## Возможности

- Выбор случайной жертвы дня (с учётом исключённых, доверенных и шансов для владельца)
- Поддержка доверенных пикеров (можно делегировать право запуска жеребьёвки)
- Исключения: можно временно не включать кого-то в жеребьёвку
- Управление напоминаниями, временем жеребьёвки, лимитами и шансами
- Добавление/удаление собственных фраз, управление источником фраз
- Статистика по каждому чату: кто сколько раз был “жертвой дня”
- Поддержка автотестов и CI (GitHub Actions)

## Установка и запуск

### 1. Клонируй репозиторий

```bash
git clone https://github.com/yourusername/tg-victim-bot.git
cd tg-victim-bot
```

### 2. Подготовь переменные окружения

Создай файл `.env` в корне проекта:

```ini
BOT_TOKEN=ваш_токен_бота_от_BotFather
```

### 3. Установи зависимости

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 4. Запусти бота

```bash
python victim_bot.py
```

или через Docker:

```bash
docker build -t tg-victim-bot .
docker run --env-file .env tg-victim-bot
```

### 5. Добавь бота в свой чат и выдай права администратора

- Добавь бота в группу и выдай ему права администратора (можно без права удалять пользователей).
- Пропиши команды через BotFather (см. FAQ ниже).

### 6. Проверь работу команд

- `/start`, `/help` — справка
- `/victim` — запуск жеребьёвки (разрешено владельцу и доверенным пикерам)
- `/statistics` — посмотреть статистику
- ... (см. ниже для полного списка)

## Основные команды

| Команда                  | Описание                          | Кто может       |
| ------------------------ | --------------------------------- | --------------- |
| `/start`                 | Краткое приветствие               | Все             |
| `/help`                  | Справка и все команды             | Все             |
| `/victim`                | Запуск жеребьёвки                 | Владелец, пикер |
| `/statistics`            | Статистика по “жертвам”           | Все             |
| `/set_limit`             | Установить лимит жеребьёвок       | Владелец, пикер |
| `/add_picker`            | Добавить доверенного пикера       | Владелец, пикер |
| `/del_picker`            | Удалить доверенного пикера        | Владелец, пикер |
| `/list_pickers`          | Список доверенных пикеров         | Все             |
| `/chance_owner`          | Изменить шанс выпадения владельца | Владелец, пикер |
| `/reminder_off`          | Отключить напоминания на N дней   | Владелец, пикер |
| `/reminder_on`           | Включить напоминания              | Владелец, пикер |
| `/reminder_time`         | Изменить время напоминания        | Владелец, пикер |
| `/reminder_weekends_on`  | Включить напоминания по выходным  | Владелец, пикер |
| `/reminder_weekends_off` | Отключить напоминания по выходным | Владелец, пикер |
| `/exclude`               | Исключить участника               | Владелец, пикер |
| `/include`               | Включить участника                | Владелец, пикер |
| `/list_excluded`         | Показать исключённых              | Все             |
| `/phrases_source`        | Управлять источником фраз         | Владелец, пикер |
| `/add_phrase`            | Добавить свою фразу               | Владелец, пикер |
| `/del_phrase`            | Удалить фразу                     | Владелец, пикер |
| `/list_phrases`          | Показать все фразы                | Все             |

## Кастомные фразы и подстановка имени пользователя

- Во всех фразах (и кастомных, и стандартных) можно использовать переменную `{mention}`.
- `{mention}` автоматически заменится на имя выбранного пользователя с кликабельным упоминанием (`@username` или “Имя Фамилия”).
- Пример фразы:
  ```
  Сегодня жертвой дня становится {mention}! Готовься выполнять ТЗ до утра.
  ```
- Если не использовать `{mention}`, имя пользователя не появится в сообщении.

## Сообщения об ошибке доступа для обычного участника

| Команда                  | Сообщение для обычного участника                               |
| ------------------------ | -------------------------------------------------------------- |
| `/victim`                | Только @владелец, @доверенные могут назначать жертву!          |
| `/set_limit`             | Только владелец или доверенный может менять лимит жеребьёвок!  |
| `/add_picker`            | Только владелец или доверенный может добавлять пикеров.        |
| `/del_picker`            | Только владелец или доверенный может удалять пикеров.          |
| `/chance_owner`          | Только владелец или доверенный может менять шансы.             |
| `/reminder_off`          | Только владелец или доверенный может управлять напоминаниями!  |
| `/reminder_on`           | Только владелец или доверенный может управлять напоминаниями!  |
| `/reminder_time`         | Только владелец или доверенный может менять время напоминаний! |
| `/reminder_weekends_on`  | Только владелец или доверенный может управлять напоминаниями!  |
| `/reminder_weekends_off` | Только владелец или доверенный может управлять напоминаниями!  |
| `/exclude`               | Только владелец или доверенный может исключать участников.     |
| `/include`               | Только владелец или доверенный может включать участников.      |
| `/phrases_source`        | Только владелец или доверенный может менять источник фраз.     |
| `/add_phrase`            | Только владелец или доверенный может добавлять фразы.          |
| `/del_phrase`            | Только владелец или доверенный может удалять фразы.            |

---

## FAQ

1. Открой [@BotFather](https://t.me/BotFather), выбери своего бота → Bot Settings → Commands.
2. Введи список команд и их описания (см. раздел “Основные команды” выше).
3. Сохрани.

- Проверь, что в `.env` корректный токен (`BOT_TOKEN=...`)
- Проверь логи Amvera или Docker
- Бот должен быть админом в чате!

Чтобы бот автоматически подставлял имя выбранного пользователя в вашу кастомную фразу, используйте шаблон `{mention}` — например:

```
{mention}, сегодня твой день! Готовься к новым задачам.
```

Бот заменит `{mention}` на @username или имя пользователя с кликабельным упоминанием.

## CI и автотесты

- Запуск автотестов:
  ```
  pytest test_bot.py
  ```
- При каждом push тесты автоматически прогоняются через GitHub Actions (`.github/workflows/python-app.yml`).

## Лицензия

MIT License

---

**Вопросы и улучшения — приветствуются!**

